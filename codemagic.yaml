workflows:
  ios-ipa:
    name: SEN GP iOS IPA
    max_build_duration: 60
    instance_type: mac_mini_m2
    integrations:
      # Name of the App Store Connect API key integration you add under
      # Codemagic → Team settings → Integrations → Apple Developer Portal.
      # Rename here to match whatever you call it there.
      app_store_connect: codemagic
    environment:
      ios_signing:
        # ad_hoc = installable .ipa without going through TestFlight (needs
        # device UDIDs registered in the Apple Developer portal).
        # Switch to app_store once you're ready to submit to TestFlight/App Store.
        distribution_type: ad_hoc
        bundle_identifier: com.sengp.app
      vars:
        XCODE_WORKSPACE: "frontend/ios/App/App.xcworkspace"
        XCODE_SCHEME: "App"
      node: 20
      xcode: latest
      cocoapods: default
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: deploy/aws-android-prep
          include: true
      cancel_previous_builds: true
    scripts:
      - name: Install npm dependencies
        script: |
          cd frontend
          npm install

      - name: Build web assets (www)
        script: |
          cd frontend
          npm run build

      - name: Add iOS platform
        script: |
          cd frontend
          npx cap add ios

      - name: Patch Info.plist (camera/mic/location/photos + sengp:// scheme)
        script: |
          cd frontend
          node scripts/patch-ios.js

      - name: Sync Capacitor (copies web assets, installs pods)
        script: |
          cd frontend
          npx cap sync ios

      - name: Set up code signing
        script: |
          xcode-project use-profiles

      - name: Increment build number
        script: |
          cd frontend/ios/App
          agvtool new-version -all $BUILD_NUMBER

      - name: Build ipa
        script: |
          xcode-project build-ipa \
            --workspace "$XCODE_WORKSPACE" \
            --scheme "$XCODE_SCHEME"

    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log

    publishing:
      email:
        recipients:
          - ahatcisse@gmail.com
        notify:
          success: true
          failure: true
